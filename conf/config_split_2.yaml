# Hydra configuration for RNA GNN training
# Usage: python train.py
# Override any field: python train.py model.architecture=gat training.epochs=200

experiment_name: ${model.architecture}_${features.use_nucleotide}_${features.use_structure_annotation}_${features.use_pseudoknot}_${features.use_position_encoding}_split_2_${now:%Y%m%d_%H%M%S}
output_dir: results/runs
test: false
seed: 42

# Feature configuration
features:
  use_nucleotide: true
  use_structure_annotation: true  # Motif annotation (E/S/H/I/M/B/X)
  use_pseudoknot: true
  use_position_encoding: true

# Model configuration
model:
  architecture: gin  # Options: gcn, gat, gin, gcn_edge, gat_edge, gin_edge
  hidden_dim: 128
  num_layers: 3
  dropout: 0.3
  pooling: mean  # Options: mean, max
  num_heads: 4  # Only used for GAT

# Training configuration
training:
  epochs: 100
  batch_size: 32
  lr: 0.001
  weight_decay: 0.00001
  use_class_weights: true
  patience: 20  # LR scheduler patience
  early_stop_patience: 30  # Early stopping patience

# Data paths
data:
  train_path: /home/jeff/ribozyme/data/splits/train_labels_2.json
  val_path: /home/jeff/ribozyme/data/splits/val_labels_2.json
  test_path: /home/jeff/ribozyme/data/splits/test_labels_2.json
  rfam_types_path: rfam/rfam_types_full.pkl
  st_files_dir: data/unzipped/bpRNA_1m_90_STAFILES
