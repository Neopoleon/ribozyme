# @package _global_
# Ablation study: Remove structure annotation (motif features)

experiment_name: ablation_no_structure_annotation

features:
  use_nucleotide: true
  use_structure_annotation: false  # DISABLED for ablation
  use_pseudoknot: true
  use_position_encoding: true