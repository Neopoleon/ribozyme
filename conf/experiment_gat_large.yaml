# @package _global_
# GAT model with larger hidden dimension

experiment_name: gat_large_256dim

model:
  architecture: gat
  hidden_dim: 256
  num_layers: 4
  dropout: 0.4
  pooling: mean
  num_heads: 8

training:
  epochs: 150
  batch_size: 16  # Smaller batch for larger model
  lr: 0.0005